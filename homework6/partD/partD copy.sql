/ as sysdba

DROP USER c##lennin CASCADE;
CREATE USER c##lennin IDENTIFIED BY 123;
GRANT CONNECT, RESOURCE, DBA TO c##lennin;
ALTER USER c##lennin QUOTA 100M on users;
CONNECT c##lennin/123;

CREATE TABLE COURSE(
    courseid NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    description VARCHAR(10),
    status VARCHAR(10) CHECK(Status IN ('OPEN', 'CLOSED')),
    CONSTRAINT courseid_pk PRIMARY KEY (courseid)
    );

INSERT INTO COURSE VALUES (10, 'COCONUT', 'OPEN');

DESC COURSE;

UPDATE COURSE SET description = 'My goood' WHERE courseid = 10;

SELECT * FROM COURSE;